# ─────────────────────────────────────────────────────────────────
#  requirements.txt  —  Jewellery Mortgage API
#  Exact versions pinned from local environment.
#  Install:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# Core framework
Django==4.2.30
djangorestframework==3.17.1
django-cors-headers==4.9.0

# Database
psycopg2-binary==2.9.12       # PostgreSQL driver (binary wheels, no C build needed)

# Environment / configuration
python-dotenv==1.2.2

# Production server (WSGI)
gunicorn==26.0.0

# Static file serving (production; no Nginx needed for small deployments)
whitenoise==6.12.0

# Image handling (jewellery item photos)
Pillow==11.2.1
